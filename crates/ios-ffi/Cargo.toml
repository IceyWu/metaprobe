[package]
name = "metaprobe-ios-ffi"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/IceyWu/metaprobe"
description = "C ABI for using metaprobe from iOS and Swift"

[lib]
crate-type = ["staticlib"]

[dependencies]
metaprobe-core = { path = "../core" }
serde_json = "1"
